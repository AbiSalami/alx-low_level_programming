#include <stdio.h>

/**
 * main - functions that print a word, followed by a new line
 *
 * Return: 0 when successfull
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
